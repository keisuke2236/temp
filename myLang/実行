getVAlue 評価せよという名前（実行せよ)
そのノードごとの意味に沿って実行せよ。

programnode 
　古文のステートメントリストにgetバリュー

stmtlist
  ステートメントまたはブロックに順番にgetvalue
  
stmt
  代入文そのものを実行する
  a=1
  これを実行するとは
  aという変数を作る,parseの中で作ってあることが前提
  代入するまえに右辺を計算しないといけないので以下のexprを実行する。
  
expr
  次に右辺expr計算をする。
  式を計算すると結果として値が得られる
  exprNodeのgetValueは式を計算した結果をValue型で返す。
  Value型は数値でも文字でもなんでも型付きで保存できる（ありがたや)
  式のノードにgetバリューで値としてvalueが返ってくるので
  parseで作った変数に代入する。
  
  
cond
  条件文
  getValueでValueで帰ってくるけど
  中にはしっかりboolean型でtrueまたはfalseで入っているようにする。
  


できれば１つがいい
hashmapでどの変数がどうなっているかを保存するべきである？



この言語の仕様
最後は改行で終了しないといけない
変数に数値は含めない
　flg1 flg2などのダメダメ変数ができるから


if a < 5 then
a = 5
else
a =  4
endif  

IF文 elseを書いたら改行必須
なおelseifはない

変数は文字列で検索してValue型で帰ってくること。


実行例
leo = 4
if leo > 38 then
yui = 9
else
yuireo = 69
endif
yamada = 50

do while a < 5
rorensu = 100
loop
